# virtualDesktopSwitcher
Hacky autohotkey script to swap virtual desktops quickly even if keyboard is trapped by windows RDP client.

Use Alt+Number where Number > 0 and <= 5 to go to virtual desktop directly.
